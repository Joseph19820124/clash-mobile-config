# 📱 iPhone Clash 配置导入指南

快速导入配置到你的 iPhone 代理客户端。

---

## 🎯 一键导入链接

复制下面的链接到你的 iPhone 客户端：

```
https://raw.githubusercontent.com/Joseph19820124/clash-mobile-config/main/config.yaml
```

---

## 📲 按客户端分类导入

### 1️⃣ **Stash**（推荐 ⭐⭐⭐⭐⭐）

**优点：** 最稳定的 Clash 实现，功能完整，UI 友好

**导入步骤：**

1. 打开 **Stash** 应用
2. 点击底部 **"配置"** 标签页
3. 点击右上角 **"+"** 按钮
4. 选择 **"从 URL 导入"**
5. 粘贴配置链接：
   ```
   https://raw.githubusercontent.com/Joseph19820124/clash-mobile-config/main/config.yaml
   ```
6. 点击 **"导入"** → **"完成"**
7. 在列表中点击配置，选择 **"激活"**

**更新配置：**
- 配置列表 → 向左滑动配置 → **"更新"**
- 或启用 **"自动更新"**（推荐）

---

### 2️⃣ **Shadowrocket**

**优点：** 功能完整，价格便宜（$2.99）

**导入步骤：**

1. 打开 **Shadowrocket**
2. 点击左上角 **"三"** 按钮（菜单）
3. 点击 **"配置"**
4. 点击右上角 **"+"** 按钮
5. 选择 **"从 URL 导入"**
6. 粘贴链接：
   ```
   https://raw.githubusercontent.com/Joseph19820124/clash-mobile-config/main/config.yaml
   ```
7. 点击 **"完成"** → **"保存"**
8. 返回主界面，点击配置名称使其 **"活跃"**（绿色）

**编辑配置：**
- 配置列表 → 点击配置 → **"编辑"**

---

### 3️⃣ **Surge**

**优点：** 功能最完整，规则和插件最全

**导入步骤：**

1. 打开 **Surge** 应用
2. 点击 **"配置"** 标签页
3. 点击 **"+"** 按钮
4. 选择 **"从 URL 导入"**
5. 粘贴链接：
   ```
   https://raw.githubusercontent.com/Joseph19820124/clash-mobile-config/main/config.yaml
   ```
6. 点击 **"完成"**
7. 启用配置，打开 **"增强模式"**

**特殊说明：** Surge 配置语法略有不同，可能需要手动调整某些高级选项。

---

### 4️⃣ **Quantumult X**

**优点：** 功能丰富，支持复杂规则

**导入步骤：**

1. 打开 **Quantumult X**
2. 点击左下角 **"风车"** 按钮（设置）
3. 点击 **"订阅管理"**
4. 点击 **"添加订阅"**
5. 粘贴链接，选择 **"从 URL 获取"**
6. 点击 **"完成"**
7. 返回主界面，选中配置并点击 **"下载"**

---

## 💾 方法 2：本地文件导入

### 通过 iCloud Drive

1. **在 Mac 或 PC 上：**
   ```bash
   # 克隆仓库
   git clone https://github.com/Joseph19820124/clash-mobile-config.git
   ```

2. **将 `config.yaml` 上传到 iCloud Drive**
   ```
   iCloud Drive/
   └── clash-config/
       └── config.yaml
   ```

3. **在 iPhone 上：**
   - 打开 **Files** 应用 → **iCloud Drive**
   - 找到 `config.yaml`
   - 点击 → **"用...打开"** → 选择 **Stash/Shadowrocket**

### 通过 AirDrop

1. **Mac 上：**
   ```bash
   # 找到 config.yaml
   open ~/Downloads/clash-mobile-config/
   ```

2. **右键点击 `config.yaml` → AirDrop → 选择 iPhone**

3. **iPhone 上：**
   - 接收 AirDrop
   - 选择 **"用 Stash 打开"** 或相应客户端
   - 确认导入

### 通过邮件

1. **将 `config.yaml` 作为附件发送给自己**
2. **在 iPhone 上打开邮件 → 点击附件**
3. **选择 **"用...打开"** → 选择代理客户端**

---

## ⚙️ 导入后的设置

### 启用代理

1. **打开 iPhone 设置 → 通用 → VPN 与设备管理**
2. **找到你的代理应用（Stash/Shadowrocket）**
3. **点击 → 允许访问通知 / 位置 等权限**
4. **返回代理应用，点击 "连接" / "启用"**

### 连接确认

1. **代理应用会弹出 VPN 配置确认**
2. **点击 "允许"**
3. **iPhone 状态栏会显示 VPN 图标 🔒**

### 验证连接

- 打开浏览器访问 https://ipinfo.io
- 应该看到代理节点的 IP 地址
- 如果显示本地 IP，说明没有正确连接

---

## 🔄 更新配置

### 自动更新（推荐）

**Stash：**
- 配置详情 → **"自动更新"** → 选择更新间隔

**Shadowrocket：**
- 配置列表 → 向左滑动 → **"更新"**
- 设置定时更新（在设置中）

### 手动更新

1. 打开代理应用
2. 进入配置管理
3. 点击 **"更新"** 或 **"刷新"** 按钮
4. 等待更新完成

---

## 📋 导入前的检查清单

- [ ] 已复制正确的配置 URL
- [ ] iPhone 连接到 WiFi 或有流量
- [ ] 已安装代理应用（Stash / Shadowrocket 等）
- [ ] 准备好接收 VPN 配置提示

---

## 🆘 常见问题

### Q: 导入失败，显示 "无效的配置"

**A:** 
1. 检查 URL 是否正确复制（不要有多余空格）
2. 尝试清除应用缓存后重新导入
3. 确保 iPhone 网络连接正常
4. 尝试使用其他导入方式（本地文件或邮件）

### Q: 导入成功但无法连接

**A:**
1. 检查代理节点信息是否正确（服务器地址、端口、密码）
2. 在应用中运行 "延迟测试"（检查节点是否可用）
3. 确认 VPN 权限已启用
4. 尝试重启应用或重启 iPhone

### Q: 配置在 Mac 上可用，但在 iPhone 上不行

**A:**
1. Clash 配置在不同客户端间可能有兼容性问题
2. 尝试简化配置（移除复杂的规则或插件）
3. 使用最新版本的代理客户端
4. 查看应用日志寻找具体错误信息

### Q: 如何修改代理节点？

**A:**
1. **方法 1：** 在代理应用中编辑配置
   - 配置详情 → 编辑 → 修改 proxies 部分
2. **方法 2：** 编辑本地配置文件后重新导入
3. **方法 3：** 使用订阅链接（节点会自动更新）

### Q: 怎样导入订阅？

**A:**
1. 在代理应用中找到 "订阅" 或 "节点订阅" 选项
2. 点击 "添加" → 粘贴订阅链接
3. 应用会自动获取最新节点列表
4. 在配置中引用这些节点

---

## 📞 需要帮助？

- **查看配置文件：** [config.yaml](./config.yaml)
- **查看完整说明：** [README.md](./README.md)
- **提交问题：** https://github.com/Joseph19820124/clash-mobile-config/issues

---

**提示：** 建议启用 "自动更新"，这样规则和节点会定期更新，无需手动操作。

